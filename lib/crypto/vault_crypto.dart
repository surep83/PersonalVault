/// Minimal VaultCrypto stub for background initialization.
/// Replace with the real crypto initialization logic when available.
class VaultCrypto {
	Future<void> initDb() async {
		// initialize crypto backends / keys (stub)
		return;
	}
}
